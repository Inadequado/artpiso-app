# === Dependencias ===
node_modules/

# === Build do App (Vite) ===
# Obs: NAO ignorar GSAP/dist e GSAP/esm — sao a lib publicada usada via "gsap": "file:../../GSAP"
Art Piso/App/dist/
Art Piso/App/dist-ssr/
# Build de comparacao A/B em teste de performance (temporario)
Art Piso/App/dist-antes/

# === Logs ===
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# === Arquivos locais / ambiente ===
*.local
.env
.env.*
# Gabarito SEM segredos (o .env.local real continua fora do git)
!.env.example

# === Ferramentas Claude (reinstalaveis, pesadas) ===
.claude/skills/
# Permissoes/config por maquina (o settings.json compartilhado continua versionado)
.claude/settings.local.json
# Worktrees temporarias criadas por agentes
.claude/worktrees/

# === Editores ===
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# === Sistema operacional ===
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
